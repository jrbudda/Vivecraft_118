--- a/optifine/OptiFineTransformationService.java
+++ b/optifine/OptiFineTransformationService.java
@@ -138,6 +138,7 @@
 
     public static Optional<URL> getResourceUrl(String name)
     {
+        LOGGER.info("req: " + name);
         if (name.endsWith(".class") && !name.startsWith("optifine/"))
         {
             name = "srg/" + name;
